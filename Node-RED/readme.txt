All logic on thegrowingdude Controller is handled by Node-RED. It takes sensor values from the Arduinos via the MQTT\Mosquitto broker running on the RaspberryPi, interperts them in a funtion, and then returns binary values back to the Arduino via MQTT to turn diffrent relays on\off. 

/thegrowingdude
